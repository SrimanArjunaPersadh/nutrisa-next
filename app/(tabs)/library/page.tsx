import { BookOpen } from "lucide-react";
import { SurfaceStub } from "@/components/surface-stub";

export default function LibraryPage() {
  return (
    <SurfaceStub
      title="Library"
      icon={BookOpen}
      arrives="Saved foods and the meal builder land in Phase 5."
    />
  );
}
