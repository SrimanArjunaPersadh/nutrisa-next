import { Utensils } from "lucide-react";
import { SurfaceStub } from "@/components/surface-stub";

export default function NutritionPage() {
  return (
    <SurfaceStub
      title="Nutrition"
      icon={Utensils}
      arrives="Macro tiles, logged meals and the water tracker land in Phase 4."
    />
  );
}
