import { TrendingDown } from "lucide-react";
import { SurfaceStub } from "@/components/surface-stub";

export default function WeightPage() {
  return (
    <SurfaceStub
      title="Weight"
      icon={TrendingDown}
      arrives="The trend chart and weight history land in Phase 3."
    />
  );
}
